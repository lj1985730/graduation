<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- 使@Resource生效，使用 context:component-scan 可忽略 -->
    <context:annotation-config />

	<!-- 校验注解 -->
	<bean id="validator" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean" />
   <!--  <bean id="mapJacksonHttpMessageConverter" class="com.landsea.system.util.MapJacksonHttpMessageConverter" />
    <bean id="entityJacksonHttpMessageConverter" class="com.landsea.system.util.EntityJacksonHttpMessageConverter" /> -->
	<mvc:annotation-driven validator="validator" >
		<!-- <mvc:message-converters>
	      <ref bean="mapJacksonHttpMessageConverter" />
	      <ref bean="entityJacksonHttpMessageConverter" />
	    </mvc:message-converters>  -->
    </mvc:annotation-driven>

    <!-- 国际化视图解析器 -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="viewClass" value="com.landsea.system.util.view.JstlView" />
        <property name="prefix" value="/WEB-INF/templates/" />
        <property name="suffix" value=".jsp" />
        <property name="order" value="0" />
    </bean>

    <!-- 初始化bean上下文帮助类 -->
    <bean class="com.landsea.core.util.bean.SpringContextUtil" />

	<!-- 可追加配置二级缓存 -->
    <bean id="sessionFactory" class="org.springframework.orm.hibernate5.LocalSessionFactoryBean" >
        <property name="dataSource" ref="dataSource" />
        <property name="packagesToScan">
            <list>
                <value>com.landsea.**.entity</value>
            </list>
        </property>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${db.dialect}</prop>
                <prop key="hibernate.show_sql">false</prop>
                <prop key="hibernate.format_sql">true</prop>
                <prop key="hibernate.query.substitutions">true 1, false 0</prop>
                <prop key="hibernate.default_batch_fetch_size">16</prop>
                <prop key="hibernate.jdbc.batch_size">50</prop>
                <prop key="hibernate.max_fetch_depth">2</prop>
                <prop key="hibernate.generate_statistics">false</prop>
                <prop key="hibernate.bytecode.use_reflection_optimizer">true</prop>
                <prop key="current_session_context_class">thread</prop>
            </props>
        </property>
    </bean>

    <!-- aop注解支持 -->
    <aop:aspectj-autoproxy proxy-target-class="true" />

    <!-- 事务管理,声明式事务管理 -->
	<bean id="transactionManager" class="org.springframework.orm.hibernate5.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>
	<tx:annotation-driven transaction-manager="transactionManager" />

	<!-- SpringMVC上传文件时，需要配置MultipartResolver处理器 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
        <!-- 指定所上传文件的总大小不能超过20000KB。注意maxUploadSize属性的限制不是针对单个文件，而是所有文件的容量之和 -->
        <property name="maxUploadSize" value="20000000"/>
    </bean>

    <!-- 统一异常处理 -->
    <!-- SpringMVC在超出上传文件限制时，会抛出org.springframework.web.multipart.MaxUploadSizeExceededException -->
    <!-- 该异常是SpringMVC在检查上传的文件信息时抛出来的，而且此时还没有进入到Controller方法中 -->
    <bean id="exceptionResolver" class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
		<!-- 特殊处理异常 -->
        <property name="exceptionMappings">
            <props>
                <!-- 遇到MaxUploadSizeExceededException异常时，自动跳转到/WEB-INF/jsp/error_fileupload.jsp页面 -->
                <prop key="org.springframework.web.multipart.MaxUploadSizeExceededException">/error</prop>
            </props>
        </property>
        <property name="warnLogCategory" value="WARN" />
    </bean>

    <!-- 配置国际化资源文件路径 -->
	<bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource">
		<property name="basename" value="messages/message" />
	</bean>
	<!-- 基于Cookie的本地化解析器 -->
	 <bean id="localeResolver" class="org.springframework.web.servlet.i18n.CookieLocaleResolver">
	 	<property name="cookieMaxAge" value="604800"/>
	 	<property name="defaultLocale" value="zh_CN"/>
	 	<property name="cookieName" value="Language" />
	 </bean>

    <!-- 请求响应拦截器 -->
    <mvc:interceptors>
    	<!-- 拦截器执行控制器拦截，用于cookie身份和权限验证 -->
        <mvc:interceptor>
			<mvc:mapping path="/**" />
			<mvc:exclude-mapping path="/"/>
			<mvc:exclude-mapping path="/register"/>
			<mvc:exclude-mapping path="/downloadCloudFile/**"/>
			<mvc:exclude-mapping path="/register/**"/>
			<mvc:exclude-mapping path="/mail/**"/>
            <mvc:exclude-mapping path="/login"/>
            <mvc:exclude-mapping path="/loginback"/>
            <mvc:exclude-mapping path="/companyLogin"/>
            <mvc:exclude-mapping path="/api/login"/>
            <mvc:exclude-mapping path="/logout"/>
            <mvc:exclude-mapping path="/portalLogout"/>
            <mvc:exclude-mapping path="/sysdate"/>
            <mvc:exclude-mapping path="/error"/>
            <mvc:exclude-mapping path="/authors"/>
			<mvc:exclude-mapping path="/top"/>
			<mvc:exclude-mapping path="/bottom"/>
            <mvc:exclude-mapping path="/templates/**"/>
            <mvc:exclude-mapping path="/plugins/**"/>
            <mvc:exclude-mapping path="/scripts/**"/>
            <mvc:exclude-mapping path="/css/**"/>
            <mvc:exclude-mapping path="/temp/**"/>
            <mvc:exclude-mapping path="/images/**"/>
            <mvc:exclude-mapping path="/messages/**"/>
            <mvc:exclude-mapping path="/frontend/**"/>
            <mvc:exclude-mapping path="/admin"/>
            <mvc:exclude-mapping path="/front/**"/>
            <mvc:exclude-mapping path="/userfiles/**"/>
            <mvc:exclude-mapping path="/upfiles/**"/>
            <mvc:exclude-mapping path="/system/file/sysFile/getImageofBusinessId/**"/>
            <mvc:exclude-mapping path="/system/file/sysFile/getImageofFolderFileId/**"/>
            <mvc:exclude-mapping path="/system/util/showPdf/**"/>
            <mvc:exclude-mapping path="/scidownload/**"/>
            <mvc:exclude-mapping path="/websocketSCI/**"/>
            <mvc:exclude-mapping path="/webserviceSCI/**"/>
            <mvc:exclude-mapping path="/seamapSCI/**"/>
            <mvc:exclude-mapping path="/lsWebsocket/authorized"/>
            <mvc:exclude-mapping path="/lsWebsocket/unauthorized"/>

            <bean class="com.landsea.system.util.SysInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 加载各个模块中的spring配置文件 -->
    <import resource="${context.import}" />
</beans>